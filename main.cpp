#include <iostream>

int main()
{
    std:: cout << "You are a secret agent breaking into a secure server room\n";
    // std:: cout << std::endl

    std::cout << "You need to enter the correct codes to continue";
    /* code */

    const int CodeA = 0;
    const int CodeB = 1;
    const int CodeC = 2;

    const int sum = a+b+c;
    const int product = a*b*c;
    if (a != b)
    {
      std::cout << "Nothing happened";  /* code */
    }
    
    
    return a;
}
